# EasyOCR API - GPU Optimized Dependencies
# Minimal production dependencies for NVIDIA GPU acceleration

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.1

# OCR engine (EasyOCR with PyTorch CUDA support)
easyocr==1.7.2
torch==2.1.2
torchvision==0.16.2

# PDF generation and handling
PyMuPDF==1.23.26

# Image processing
Pillow==10.4.0
opencv-python-headless==4.10.0.84

# Form-data uploads
python-multipart==0.0.9

# HTTP client for health checks
requests==2.31.0

# Numpy (explicitly specify for compatibility)
numpy==1.24.3
